
const Details = () => {
    return (
        <div>
            <h1>this is singleData</h1>
            {/* function MyComponent({ routeParameter }) {
  const [data, setData] = useState(null);

  useEffect(() => {
    // Define an async function to fetch data
    async function fetchData() {
      try {
        const response = await fetch(`https://example.com/api/${routeParameter}`);
        const data = await response.json();
        setData(data);
      } catch (error) {
        console.error(error);
      }
    }

    fetchData(); // Invoke the fetch data function
  }, [routeParameter]); // Run useEffect whenever routeParameter changes

  // Render your component
  return (
    <div>
      {/* Display the fetched data */}
      {data && <p>{data}</p>}
    </div>
  );
} */}

        </div>
    );
};

export default Details;